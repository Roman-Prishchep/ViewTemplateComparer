<Window x:Class="ViewTemplateComparer.ComparisonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Сравнение шаблонов вида" Height="250" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Фильтр по типу вида:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <ComboBox x:Name="ViewTypeFilterComboBox" Grid.Row="0" Grid.Column="1" Margin="0,5" SelectionChanged="ViewTypeFilterComboBox_SelectionChanged"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Шаблон 1:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <ComboBox x:Name="Template1ComboBox" Grid.Row="1" Grid.Column="1" Margin="0,5" SelectionChanged="TemplateSelectionChanged"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Шаблон 2:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <ComboBox x:Name="Template2ComboBox" Grid.Row="2" Grid.Column="1" Margin="0,5" SelectionChanged="TemplateSelectionChanged"/>

        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CompareButton" Content="Сравнить" Width="100" Margin="0,0,10,0" IsEnabled="False" Click="CompareButton_Click"/>
            <Button Content="Отмена" Width="100" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>